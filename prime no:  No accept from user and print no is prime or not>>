#include<stdio.h>
void main()
{
    int n,i=2;
    printf("enter the no: ");
    scanf("%d",&n);
    for(i=2;i<=n-1;i++)
    {
        if(n%i==0)
        {
        printf("%d is not prime",n);
        break;
        }
    }
    if(i==n)
    {
    printf("%d is prime",n);
}}
